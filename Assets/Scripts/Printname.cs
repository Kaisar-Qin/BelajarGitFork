using UnityEngine;

public class Printname : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Nama saya Kaisar deno ankabut");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
